<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">

    <!-- My own css -->
    <link rel="stylesheet" href="<%= cssFilePath %>" />

    <title>Dr on the go</title>
  </head>
  <body>
    <!-- this navbar is for when jquery has NO connection issues -->
    <nav class="navbar fixed-top navbar-light navbar-expand shadow" style="background-color: <%= navbarColor %>;" >
      <% if(user.is_doctor === true) { %> 
        <div class="navbar-nav">
          <a
            class="nav-link dropdown-toggle active"
            href="#"
            role="button"
            data-toggle="dropdown"
          >Dashboard</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="/patient-dashboard">Patient</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="/doctor-dashboard">Doctor</a>
          </div>
          <span class="navbar-text">
            <% if(navbarColor === '#e3f2fd') { %>
              patient mode
            <% } else { %>
              doctor mode 
            <% }; %>
          </span>
        </div>
      <% } else { %> 
        <a href="/patient-dashboard" class="navbar-brand">Dashboard</a>
      <% }; %>

      <div class= 'navbar-nav ml-auto'>
        <div class="nav-item dropdown">
          <a
            class="nav-link dropdown active pt-0 pb-0"
            href="#"
            role="button"
            data-toggle="dropdown"
          >
            <span class="navbar-text" id="profile-photo-span">
              <img class="rounded-circle border img-fluid" id="profile-photo-img" src="<%= user.photo %>"/>
            </span>
          </a>
          <div class="dropdown-menu dropdown-menu-right">
            <a class="dropdown-item" href="/profile">View profile</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="/logout">Logout</a>
          </div>
        </div>
      </div>
      
    </nav>

    <!-- ----------------------------------------------------------------------- -->
    
    <!-- this navbar is for when jquery has connection issues -->
    <!-- <nav class="navbar fixed-top navbar-light navbar-expand-sm shadow" style="background-color: #e3f2fd;">
      <% if(user.is_doctor === true) { %> 
        <a href="/patient-dashboard" class="navbar-brand">Patient View</a>
        <a href="/doctor-dashboard" class="navbar-brand">Doctor View</a>
      <% } else { %> 
        <a href="/patient-dashboard" class="navbar-brand">Dashboard</a>
      <% }; %>
      
      <div class="navbar-nav ml-auto">
        <span class="navbar-text" id="profile-photo-span">
          <img class="rounded-circle border border-secondary img-fluid" src="/profile-photos/<%= user.photo %>"/>
        </span>
        <a class="nav-link" href="/profile">View Profile</a>
        <a class="nav-link" href="/logout">Logout</a>
      </div>
    </nav> -->